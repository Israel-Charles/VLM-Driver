# VLM-Driver

Run the model with: 
```modal deploy model.py```

Stop it with:
```modal app stop example-sglang-low-latency```

Change [PUBLIC_IP] to the actual ip before running query_vlm.sh
To make the script executable:

```
chmod +x query_vlm.sh
```
